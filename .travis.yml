os: linux
language: node_js
node_js:
   - '12'

branches:
   only:
      - master

before_script:
   - npm install -g @angular/cli
   - npm install

script:
   - ng build --prod
deploy:
  provider: pages
  cleanup: false
  github_token: "453fa124c4032cd2b3c4e4119cf2ddc7100248ab"
  keep_history: true
  on:
    branch: master
